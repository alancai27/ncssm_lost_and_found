flask>=3.0
pillow>=10.0
requests>=2.31
python-dotenv>=1.0
gunicorn>=21.0

# Production only. Locally the app uses SQLite and the local filesystem, and
# neither of these is imported unless DATABASE_URL / S3_BUCKET are set.
psycopg[binary]>=3.1
boto3>=1.34

# Optional: accept iPhone HEIC uploads directly.
# pillow-heif>=0.16
